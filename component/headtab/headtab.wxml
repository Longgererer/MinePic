<view class="head-tab-box" bindtap="{{headTabIndex == 0 ? 'myPicTap' : 'myFriendTap'}}">
        <image class="head-tab-image" src="{{headTabImg}}" />
        <text class="head-tab-text">{{headTabText}}</text>
</view>